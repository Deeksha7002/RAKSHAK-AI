services:
  db:
    image: postgres:15
    container_name: rakshak-postgres
    environment:
      POSTGRES_USER: rakshak
      POSTGRES_PASSWORD: rakshakpassword
      POSTGRES_DB: rakshak_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:alpine
    container_name: rakshak-redis
    ports:
      - "6379:6379"

  backend:
    build: ./backend
    container_name: rakshak-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - PYTHONUNBUFFERED=1
      - DATABASE_URL=postgresql://rakshak:rakshakpassword@db:5432/rakshak_db
      - REDIS_URL=redis://redis:6379
    depends_on:
      - db
      - redis

  frontend:
    build: ./frontend
    container_name: rakshak-frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  postgres_data:
